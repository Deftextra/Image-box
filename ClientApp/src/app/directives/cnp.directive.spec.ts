import { CnpDirective } from './cnp.directive';

describe('CnpDirective', () => {
  it('should create an instance', () => {
    const directive = new CnpDirective();
    expect(directive).toBeTruthy();
  });
});
